function addTwoNums(a, b) {
    return a + b
}

const add2Nums = (a, b) => {
    return a + b
}

const squareNum = a => {
    return a ** 2
}

// IF THE FUNCTION IS ONLY ONE LINE, 
// WE CAN LEAVE OFF THE {} AND THE RETURN

const squareNum2 = a => a ** 2 // TRAIN MYSELF TO SEE ARROW AND UNDERSTAND... FUNCTION

console.log(squareNum2(5))

const aspectRatio = (w, h) => w / h // EXAMPLE OF TODD WHEN HE WAS WORKING AT THE PHOTO COMPANY

const sayHey = () => console.log("Ho")

sayHey()